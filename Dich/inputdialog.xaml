<Window x:Class="subphimv1.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:subphimv1"
        mc:Ignorable="d"
        Title="Nhập Dữ Liệu" Height="180" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None" AllowsTransparency="True"
        Background="{StaticResource WindowBackgroundColor}"
        Foreground="{StaticResource TextColor}"
        FontFamily="Segoe UI">

    <Border BorderBrush="{StaticResource AccentColor}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="29"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown" Margin="0,0,0,11">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource WindowCloseButtonStyle}" Content="✕" Click="CloseButton_Click" Width="50" FontSize="14"/>
                </StackPanel>
            </Grid>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="20,20,20,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="lblPrompt" Text="Nhập giá trị:" Grid.Row="0" Margin="0,0,0,10" Foreground="{StaticResource TextSecondaryColor}"/>
                <TextBox x:Name="txtName" Grid.Row="1" VerticalContentAlignment="Center"/>
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                    <Button Content="Hủy" Width="80" IsCancel="True" Click="BtnCancel_Click" Margin="0,0,10,0"/>
                    <Button Content="OK" Width="80" IsDefault="True" Click="BtnOK_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>