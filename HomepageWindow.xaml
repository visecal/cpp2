<Window x:Class="subphimv1.HomepageWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:subphimv1"
        mc:Ignorable="d"
        Title="AIO Launcher - Dashboard" 
        Height="800" Width="1400"
        MinHeight="720" MinWidth="1280"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True"
        Background="Transparent"
        
        FontFamily="Segoe UI Variable Display, Segoe UI, Arial" 
        Loaded="HomepageWindow_Loaded">

    <Window.Resources>

        <!-- Colors -->
        <Color x:Key="PrimaryColor">#FF6C5CE7</Color>
        <Color x:Key="SecondaryColor">#FF00D2FF</Color>
        <Color x:Key="AccentColor">#FFFF6B6B</Color>
        <Color x:Key="SuccessColor">#FF51CF66</Color>
        <Color x:Key="WarningColor">#FFFFA502</Color>
        <Color x:Key="ErrorColor">#FFEE5A6F</Color>
        <Color x:Key="BackgroundDark">#FF0F0F1E</Color>
        <Color x:Key="BackgroundMedium">#FF1A1A2E</Color>
        <Color x:Key="BackgroundLight">#FF252541</Color>
        <Color x:Key="TextPrimary">#FFFFFFFF</Color>
        <Color x:Key="TextSecondary">#FFB8B8D1</Color>
        <Color x:Key="TextTertiary">#FF7C7C9A</Color>
        <Color x:Key="BorderColor">#FF3A3A5C</Color>
        <Color x:Key="HoverColor">#FF2D2D4A</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="{StaticResource SecondaryColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="BackgroundDarkBrush" Color="{StaticResource BackgroundDark}"/>
        <SolidColorBrush x:Key="BackgroundMediumBrush" Color="{StaticResource BackgroundMedium}"/>
        <SolidColorBrush x:Key="BackgroundLightBrush" Color="{StaticResource BackgroundLight}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimary}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondary}"/>
        <SolidColorBrush x:Key="TextTertiaryBrush" Color="{StaticResource TextTertiary}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="HoverBrush" Color="{StaticResource HoverColor}"/>

        <!-- Gradient Brushes -->
        <LinearGradientBrush x:Key="PrimaryGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource PrimaryColor}" Offset="0"/>
            <GradientStop Color="{StaticResource SecondaryColor}" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="{StaticResource BackgroundDark}" Offset="0"/>
            <GradientStop Color="{StaticResource BackgroundMedium}" Offset="1"/>
        </LinearGradientBrush>

        <LinearGradientBrush x:Key="CardGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#40252541" Offset="0"/>
            <GradientStop Color="#202D2D4A" Offset="1"/>
        </LinearGradientBrush>

        <!-- Converters -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InvertedBooleanToVisibilityConverter x:Key="InvertedBoolToVis"/>

        <!-- STYLES -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets, Segoe UI Symbol, Arial"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <TextBlock Text="{TemplateBinding Content}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#22FFFFFF"/>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="WindowCloseButtonStyle" BasedOn="{StaticResource TitleBarButtonStyle}" TargetType="Button">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ModernButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CardGradient}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.3" Color="Black"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ShimmerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource CardGradient}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                ClipToBounds="True"> <!-- ClipToBounds quan trọng để hiệu ứng không tràn ra ngoài -->
                            <Grid>
                                <!-- Nội dung gốc của Button -->
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>

                                <!-- Lớp hiệu ứng gợn sóng -->
                                <Border x:Name="shimmerEffect"
                                        HorizontalAlignment="Left"
                                        Width="80"
                                        IsHitTestVisible="False"
                                        Opacity="0.7">
                                    <Border.Background>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#00FFFFFF" Offset="0"/>
                                            <GradientStop Color="#55FFFFFF" Offset="0.5"/>
                                            <GradientStop Color="#00FFFFFF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Border.Background>
                                    <Border.RenderTransform>
                                        <!-- Bắt đầu từ vị trí ngoài màn hình bên trái -->
                                        <TranslateTransform X="-100" />
                                    </Border.RenderTransform>
                                </Border>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            
                            <!-- Trigger để chạy animation khi button được tải -->
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <DoubleAnimation
                                            Storyboard.TargetName="shimmerEffect"
                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                            From="-100" 
                                            To="200"     
                                            Duration="0:0:1.5"
                                            BeginTime="0:0:3">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseInOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Background" Value="{StaticResource PrimaryGradient}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
        </Style>
        <Style x:Key="BannerButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    CornerRadius="8"
                    SnapsToDevicePixels="True"
                    ClipToBounds="True">
                            <!-- SỬA: dùng ContentPresenter để hiển thị đúng mọi loại Content (Image/Text/StackPanel/...) -->
                            <ContentPresenter
                    Content="{TemplateBinding Content}"
                    ContentTemplate="{TemplateBinding ContentTemplate}"
                    Margin="{TemplateBinding Padding}"
                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>

        <Style x:Key="IconButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="16,12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10"
                                Margin="8,2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0" 
                                        Width="36" Height="36"
                                        Background="{StaticResource CardGradient}"
                                        CornerRadius="8"
                                        Margin="0,0,12,0">
                                    <Image Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Width="20" Height="20" RenderOptions.BitmapScalingMode="HighQuality" Opacity="0.8"/>
                                </Border>

                                <ContentPresenter Grid.Column="1" 
                                                VerticalAlignment="Center"
                                                TextBlock.FontSize="14"
                                                TextBlock.FontWeight="Medium"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                                            <GradientStop Color="#806C5CE7" Offset="0"/>
                                            <GradientStop Color="#4000D2FF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DashboardCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardGradient}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.3" Color="Black"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="StatCardStyle" TargetType="Border" BasedOn="{StaticResource DashboardCardStyle}">
            <Setter Property="Padding" Value="24,20"/>
            <Setter Property="MinWidth" Value="200"/>
            <Setter Property="MinHeight" Value="120"/>
        </Style>
        <Style x:Key="CompactStatCardStyle" TargetType="Border" BasedOn="{StaticResource DashboardCardStyle}">
            <Setter Property="Padding" Value="16,14"/>
            <Setter Property="VerticalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>

        <Style x:Key="ModernTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardGradient}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Padding="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.5" Color="{StaticResource SecondaryColor}"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Background" Value="{StaticResource CardGradient}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                        Padding="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="10" Opacity="0.5" Color="{StaticResource SecondaryColor}"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- NEW DETAILED COMBOBOX STYLE -->
        <Style x:Key="ModernComboBoxStyle" TargetType="{x:Type ComboBox}">
            <Setter Property="Background" Value="{StaticResource CardGradient}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ComboBox}">
                        <Grid>
                            <ToggleButton x:Name="ToggleButton" 
                                          Grid.Column="2" 
                                          Focusable="false"
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                          ClickMode="Press">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                                        <Border x:Name="templateRoot" 
                                                Background="{StaticResource CardGradient}"
                                                BorderBrush="{StaticResource BorderBrush}"
                                                BorderThickness="1" 
                                                CornerRadius="8">
                                            <Border x:Name="splitBorder" 
                                                    Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" 
                                                    Margin="0" 
                                                    HorizontalAlignment="Right" 
                                                    BorderThickness="1" 
                                                    BorderBrush="Transparent">
                                                <Path x:Name="arrow"
                                                      Data="F1 M 0,0 L 2.667,2.667 L 5.333,0 L 5.333,-1.78168 L 2.667,0.88501 L0,-1.78168 L0,0 Z"
                                                      Fill="{StaticResource TextTertiaryBrush}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Border>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="ContentSite"
                                              IsHitTestVisible="False" 
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                              ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                              Margin="12,10,30,10"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"/>
                            <Popup x:Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid x:Name="DropDown"
                                      SnapsToDevicePixels="True"
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                            Background="{StaticResource BackgroundLightBrush}"
                                            BorderThickness="1"
                                            CornerRadius="8"
                                            BorderBrush="{StaticResource BorderBrush}"/>
                                    <ScrollViewer Margin="4,6,4,6" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" TargetName="ToggleButton" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" TargetName="ToggleButton" Value="{StaticResource SecondaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- NEW DETAILED CHECKBOX STYLE -->
        <Style x:Key="ModernCheckBoxStyle" TargetType="{x:Type CheckBox}">
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type CheckBox}">
                        <StackPanel Orientation="Horizontal">
                            <Border x:Name="checkBoxBorder"
                                    Width="20"
                                    Height="20"
                                    CornerRadius="6"
                                    BorderThickness="1"
                                    BorderBrush="{StaticResource BorderBrush}"
                                    Background="{StaticResource CardGradient}">
                                <Grid>
                                    <TextBlock x:Name="checkMark"
                                               Text="✔"
                                               FontWeight="Bold"
                                               Foreground="White"
                                               FontSize="12"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"
                                               Opacity="0"/>
                                </Grid>
                            </Border>
                            <ContentPresenter Margin="8,0,0,0"
                                              VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkBoxBorder" Property="Background" Value="{StaticResource PrimaryGradient}"/>
                                <Setter TargetName="checkBoxBorder" Property="BorderBrush" Value="Transparent"/>
                                <Setter TargetName="checkMark" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="FeatureCardStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="MinWidth" Value="220"/>
            <Setter Property="Height" Value="110"/>
            <Setter Property="Margin" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Top"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="12"
                        Padding="{TemplateBinding Padding}">
                            <StackPanel>
                                <Image Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Width="28" Height="28" HorizontalAlignment="Left" Margin="0,0,0,10" RenderOptions.BitmapScalingMode="HighQuality"/>
                                <StackPanel>
                                    <TextBlock Text="{TemplateBinding Content}" FontWeight="SemiBold" FontSize="15" Foreground="{TemplateBinding Foreground}"/>
                                    <TextBlock Text="{TemplateBinding ToolTip}" FontSize="11" Foreground="{StaticResource TextTertiaryBrush}" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="QuickAccessButtonStyle" TargetType="Button" BasedOn="{StaticResource ModernButtonStyle}">
            <Setter Property="Width" Value="140"/>
            <Setter Property="Height" Value="90"/>
            <Setter Property="Margin" Value="8"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        CornerRadius="12"
                        Padding="{TemplateBinding Padding}">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Image Source="{Binding Tag, RelativeSource={RelativeSource TemplatedParent}}" Width="24" Height="24" Margin="0,0,0,8" RenderOptions.BitmapScalingMode="HighQuality" Opacity="0.9"/>
                                <TextBlock Text="{TemplateBinding Content}" FontWeight="Medium" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource HoverBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="ModernScrollBarThumb" TargetType="{x:Type Thumb}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Thumb}">
                        <Border x:Name="border"
                                Background="{StaticResource TextTertiaryBrush}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True"
                                Opacity="0.7"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="Opacity" Value="1"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="Opacity" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBarStyle" TargetType="{x:Type ScrollBar}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="false"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="MinWidth" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollBar}">
                        <Grid SnapsToDevicePixels="true" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="true"
                                   IsEnabled="{TemplateBinding IsMouseOver}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineUpCommand}" Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.LineDownCommand}" Opacity="0" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ModernScrollBarThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollViewerStyle" TargetType="{x:Type ScrollViewer}">
            <Setter Property="OverridesDefaultStyle" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ScrollViewer}">
                        <Grid>
                            <ScrollContentPresenter x:Name="PART_ScrollContentPresenter"
                                                    Content="{TemplateBinding Content}"
                                                    ContentTemplate="{TemplateBinding ContentTemplate}"
                                                    CanContentScroll="{TemplateBinding CanContentScroll}"
                                                    Margin="{TemplateBinding Padding}"/>

                            <ScrollBar x:Name="PART_VerticalScrollBar"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"
                                       HorizontalAlignment="Right"
                                       Style="{StaticResource ModernScrollBarStyle}"
                                       Opacity="0"/>

                            <ScrollBar x:Name="PART_HorizontalScrollBar"
                                       Orientation="Horizontal"
                                       Value="{TemplateBinding HorizontalOffset}"
                                       Maximum="{TemplateBinding ScrollableWidth}"
                                       ViewportSize="{TemplateBinding ViewportWidth}"
                                       Visibility="{TemplateBinding ComputedHorizontalScrollBarVisibility}"
                                       VerticalAlignment="Bottom"
                                       Style="{StaticResource ModernScrollBarStyle}"
                                       Height="8" MinHeight="8"
                                       Opacity="0"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                            <DoubleAnimation Storyboard.TargetName="PART_HorizontalScrollBar" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="PART_VerticalScrollBar" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.4"/>
                                            <DoubleAnimation Storyboard.TargetName="PART_HorizontalScrollBar" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.4"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="0,0,10,-10">
        <Image Source="/Resources/back.png" Stretch="UniformToFill"/>
        <Border Background="#D00F0F1E" 
                BorderBrush="#40FFFFFF"
                BorderThickness="1"
                CornerRadius="16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="60"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

            <!-- HEADER / TITLE BAR -->
            <Border Grid.Row="0" 
                    Background="#20000000"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    CornerRadius="16,16,0,0"
                    MouseDown="TitleBar_MouseDown">
                <Grid Margin="20,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="AIO LAUNCHER" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                            <TextBlock FontSize="10" Foreground="{StaticResource TextTertiaryBrush}"><Run Text="v"/><Run Text="{x:Static local:App.CurrentVersion}"/></TextBlock>
                        </StackPanel>
                            <TextBlock x:Name="ServerStatusText" 
               Text=" | Server 1 - Hoạt động"
               FontSize="15" 
               Foreground="{StaticResource TextTertiaryBrush}"
               VerticalAlignment="Bottom"
               Margin="12,0,0,2"
               Visibility="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn, Converter={StaticResource BoolToVis}}"/>
                        </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal">
                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Visibility="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn, Converter={StaticResource BoolToVis}}">
                            <Border x:Name="UpdateBadge" Background="{StaticResource WarningBrush}" CornerRadius="12" Padding="12,6" Margin="0,0,12,0" Cursor="Hand" MouseDown="UpdateNowButton_Click" Visibility="{Binding Source={x:Static local:App.Updater}, Path=IsUpdateAvailable, Converter={StaticResource BoolToVis}}">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets, Segoe UI Symbol, Arial" FontSize="14" VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock Text="Cập nhật" FontSize="12" FontWeight="SemiBold" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <Button Content="&#xE8F2;" FontFamily="Segoe MDL2 Assets, Segoe UI Symbol, Arial" FontSize="18" Style="{StaticResource ModernButtonStyle}" Padding="10" Margin="0,0,8,0" ToolTip="Cộng đồng &amp; Hỗ trợ" Click="CommunityButton_Click"/>
                            <Button x:Name="ProfileButton" Click="ProfileButton_Click" Style="{StaticResource ModernButtonStyle}" Padding="10,6" Background="{StaticResource CardGradient}">
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="32" Height="32" Background="{StaticResource PrimaryGradient}" CornerRadius="16" Margin="0,0,10,0">
                                        <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=Username, FallbackValue=U}" FontSize="14" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <StackPanel VerticalAlignment="Center">
                                        <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=Username, FallbackValue='User'}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                        <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=SubscriptionTierName, FallbackValue='Guest'}" FontSize="10" Foreground="{StaticResource TextTertiaryBrush}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Button>
                            <Popup x:Name="ProfilePopup" PlacementTarget="{Binding ElementName=ProfileButton}" Placement="Bottom" StaysOpen="False" AllowsTransparency="True">
                                <Border Background="{StaticResource BackgroundLightBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="12" Padding="12" Margin="0,8,0,0" MinWidth="250">
                                    <Border.Effect>
                                        <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.5"/>
                                    </Border.Effect>
                                    <StackPanel>
                                        <Border Background="{StaticResource CardGradient}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,8">
                                            <StackPanel>
                                                <TextBlock Text="Thông tin tài khoản" FontSize="11" FontWeight="SemiBold" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,8"/>
                                                <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"><Run Text="Gói:"/><Run Text="{Binding Source={x:Static local:App.User}, Path=SubscriptionTierName, FallbackValue='Gói'}" FontWeight="SemiBold" Foreground="{StaticResource SecondaryBrush}"/></TextBlock>
                                                <TextBlock Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" Margin="0,4,0,0" Text="{Binding Source={x:Static local:App.User}, Path=SubscriptionExpiryInfo, FallbackValue='Hết hạn: N/A'}"/>
                                            </StackPanel>
                                        </Border>
                                        <Button Content="Nâng cấp tài khoản" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Stretch" Margin="0,0,0,4" Click="UpgradeButton_Click"/>
                                        <Button Content="Đổi mật khẩu" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Stretch" Margin="0,0,0,4" Click="ChangePassword_Click"/>
                                        <Button Content="Reset thiết bị" Style="{StaticResource ModernButtonStyle}" HorizontalAlignment="Stretch" Margin="0,0,0,4" Click="ResetDevices_Click"/>
                                        <Separator Background="{StaticResource BorderBrush}" Margin="0,8"/>
                                        <Button Content="Đăng xuất" Style="{StaticResource ModernButtonStyle}" Background="{StaticResource AccentBrush}" BorderBrush="Transparent" HorizontalAlignment="Stretch" Click="LogoutButton_Click"/>
                                    </StackPanel>
                                </Border>
                            </Popup>
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                            <Button Style="{StaticResource TitleBarButtonStyle}" Content="&#xE921;" Click="MinimizeButton_Click"/>
                            <Button Style="{StaticResource TitleBarButtonStyle}" Content="&#xE922;" Click="MaximizeButton_Click"/>
                            <Button Style="{StaticResource WindowCloseButtonStyle}" Content="&#xE8BB;" Click="CloseButton_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- MAIN CONTENT AREA -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="280"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#15000000" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,12,0,0" Style="{StaticResource ModernScrollViewerStyle}">
                            <StackPanel>
                                <RadioButton x:Name="DashboardButton" Content="Dashboard" Tag="/Resources/dashboard.png" Style="{StaticResource IconButtonStyle}" GroupName="Navigation" IsChecked="True" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}" Checked="Navigation_Checked"/>
                                <RadioButton x:Name="FeaturesButton" Content="Tính Năng" Tag="/Resources/apps.png" Style="{StaticResource IconButtonStyle}" GroupName="Navigation" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}" Checked="Navigation_Checked"/>
                                <Separator Background="{StaticResource BorderBrush}" Margin="16,12" Height="1"/>
                                <Border Margin="16,20,16,12">
                                    <StackPanel>
                                        <TextBlock Text="Thông Tin Cập Nhật" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <ScrollViewer MaxHeight="250" VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewerStyle}">
                                            <TextBlock Text="{Binding Source={x:Static local:App.Updater}, Path=ReleaseNotes, FallbackValue='Đang tải thông tin...'}" TextWrapping="Wrap" LineHeight="20" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        </ScrollViewer>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>
                        <Border Grid.Row="1" Background="{StaticResource CardGradient}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0" Padding="16,12">
                            <StackPanel>
                                <TextBlock Text="Phiên bản" FontSize="10" Foreground="{StaticResource TextTertiaryBrush}"/>
                                <TextBlock FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"><Run Text="Hiện tại:"/><Run Text="{x:Static local:App.CurrentVersion}" FontWeight="SemiBold"/></TextBlock>
                                <TextBlock FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,2,0,0"><Run Text="Mới nhất:"/><Run Text="{Binding Source={x:Static local:App.Updater}, Path=LatestVersion, FallbackValue='...'}" FontWeight="SemiBold"/></TextBlock>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Border>

                <Grid Grid.Column="1">
                    <Grid Visibility="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn, Converter={StaticResource InvertedBoolToVis}}">
                        <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewerStyle}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="1" Style="{StaticResource DashboardCardStyle}" MinWidth="450" MaxWidth="500" VerticalAlignment="Center" Margin="40">
                                    <StackPanel>
                                        <!-- Login View -->
                                        <StackPanel x:Name="LoginView">
                                            <TextBlock Text="Đăng Nhập" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                            <TextBlock Text="Chào mừng trở lại! Đăng nhập để tiếp tục." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,24"/>
                                            <TextBlock Text="Tên đăng nhập hoặc Email" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <TextBox x:Name="UsernameTextBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Text="Mật khẩu" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <PasswordBox x:Name="PasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,16"/>

                                            <!-- MOVED SERVER COMBOBOX HERE -->
                                            <TextBlock Text="Chọn Server" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <ComboBox x:Name="LoginServerComboBox" Style="{StaticResource ModernComboBoxStyle}" SelectionChanged="ServerComboBox_SelectionChanged" Margin="0,0,0,16" HorizontalAlignment="left" Width="159" Height="43">
                                                <ComboBoxItem Content="Server 1"/>
                                                <ComboBoxItem Content="Server 2"/>
                                            </ComboBox>

                                            <Grid Margin="0,0,0,24">
                                                <CheckBox x:Name="RememberMeCheckBox" Content="Ghi nhớ đăng nhập" Style="{StaticResource ModernCheckBoxStyle}" HorizontalAlignment="Left" IsChecked="{Binding Source={x:Static local:App.User}, Path=RememberMe, Mode=TwoWay}"/>
                                                <Button Content="Quên mật khẩu?" Style="{StaticResource ModernButtonStyle}" Background="Transparent" BorderThickness="0" Padding="0" HorizontalAlignment="Right" Foreground="{StaticResource SecondaryBrush}" Click="SwitchToForgotPassword_Click"/>
                                            </Grid>
                                            <TextBlock x:Name="StatusMessageText" Foreground="{StaticResource ErrorBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,12" Visibility="Collapsed"/>
                                            <Button x:Name="LoginButton" Content="Đăng Nhập" Style="{StaticResource PrimaryButtonStyle}" Height="45" FontSize="15" Click="LoginButton_Click" Margin="0,0,0,16" IsDefault="True"/>
                                            <TextBlock TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"><Run Text="Chưa có tài khoản?"/><Hyperlink Click="SwitchToRegister_Click" Foreground="{StaticResource SecondaryBrush}"><Run Text="Đăng ký ngay"/></Hyperlink></TextBlock>
                                        </StackPanel>

                                        <StackPanel x:Name="RegisterView" Visibility="Collapsed">
                                            <TextBlock Text="Đăng Ký" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                            <TextBlock Text="Tạo tài khoản mới để bắt đầu sử dụng." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,24"/>
                                            <TextBlock Text="Email" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <TextBox x:Name="RegEmailTextBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Text="Tên đăng nhập" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <TextBox x:Name="RegUsernameTextBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Text="Mật khẩu (tối thiểu 6 ký tự)" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <PasswordBox x:Name="RegPasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,16"/>
                                            <TextBlock Text="Xác nhận mật khẩu" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <PasswordBox x:Name="RegConfirmPasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,16"/>

                                            <!-- MOVED SERVER COMBOBOX HERE -->
                                            <TextBlock Text="Chọn Server" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,8,0,6"/>
                                            <ComboBox x:Name="RegisterServerComboBox" Style="{StaticResource ModernComboBoxStyle}" SelectionChanged="ServerComboBox_Changed" Margin="0,0,0,16">
                                                <ComboBoxItem Content="Server 1"/>
                                                <ComboBoxItem Content="Server 2"/>
                                            </ComboBox>

                                            <TextBlock x:Name="RegStatusMessageText" Foreground="{StaticResource ErrorBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,12" Visibility="Collapsed"/>
                                            <Button x:Name="RegisterButton" Content="Đăng Ký" Style="{StaticResource PrimaryButtonStyle}" Height="45" FontSize="15" Click="RegisterButton_Click" Margin="0,8,0,16"/>
                                            <TextBlock TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"><Run Text="Đã có tài khoản?"/><Hyperlink Click="SwitchToLogin_Click" Foreground="{StaticResource SecondaryBrush}"><Run Text="Đăng nhập"/></Hyperlink></TextBlock>
                                        </StackPanel>

                                        <StackPanel x:Name="ForgotPasswordView" Visibility="Collapsed">
                                            <TextBlock Text="Quên Mật Khẩu" FontSize="32" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,12"/>
                                            <TextBlock Text="Nhập email để nhận link đặt lại mật khẩu." FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" TextAlignment="Center" Margin="0,0,0,24"/>
                                            <TextBlock Text="Email" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <TextBox x:Name="ForgotEmailTextBox" Style="{StaticResource ModernTextBoxStyle}" Margin="0,0,0,16"/>

                                            <!-- MOVED SERVER COMBOBOX HERE -->
                                            <TextBlock Text="Chọn Server" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                                            <ComboBox x:Name="ForgotServerComboBox" Style="{StaticResource ModernComboBoxStyle}" SelectionChanged="ServerComboBox_Changed" Margin="0,0,0,16">
                                                <ComboBoxItem Content="Server 1"/>
                                                <ComboBoxItem Content="Server 2"/>
                                            </ComboBox>

                                            <TextBlock x:Name="ForgotStatusMessageText" Foreground="{StaticResource ErrorBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,12" Visibility="Collapsed"/>
                                            <Button x:Name="ForgotPasswordButton" Content="Gửi Yêu Cầu" Style="{StaticResource PrimaryButtonStyle}" Height="45" FontSize="15" Click="ForgotPasswordButton_Click" Margin="0,0,0,16"/>
                                            <TextBlock TextAlignment="Center" Foreground="{StaticResource TextSecondaryBrush}" FontSize="13"><Hyperlink Click="SwitchToLogin_Click" Foreground="{StaticResource SecondaryBrush}"><Run Text="← Quay lại đăng nhập"/></Hyperlink></TextBlock>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </ScrollViewer>
                    </Grid>

                    <Grid Visibility="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn, Converter={StaticResource BoolToVis}}">

                        <!-- ===== DASHBOARD VIEW ===== -->
                            <Grid x:Name="DashboardView">
                                <ScrollViewer VerticalScrollBarVisibility="Auto" Style="{StaticResource ModernScrollViewerStyle}">
                                    <StackPanel Margin="24">
                                        <TextBlock FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,8">
                <Run Text="Xin chào,"/>
                <Run Text="{Binding Source={x:Static local:App.User}, Path=Username}" Foreground="{StaticResource SecondaryBrush}"/>
                                        </TextBlock>
                                        <TextBlock Text="Chào mừng bạn quay trở lại với AIO Launcher" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,32"/>

                                        <!-- Bảng thông tin sử dụng -->
                                        <Grid Margin="0,0,-8,-8">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Gói dịch vụ -->
                                            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource CompactStatCardStyle}">
                                                <Grid>
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="Auto"/>
                                                        <RowDefinition Height="*"/>
                                                    </Grid.RowDefinitions>
                                                    <Grid Grid.Row="0" Margin="0,0,0,8">
                                                        <TextBlock Text="Gói dịch vụ" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" VerticalAlignment="Center"/>
                                                        <Button Content="Nâng cấp" Style="{StaticResource ModernButtonStyle}" Padding="8,4" FontSize="11" Background="Transparent" BorderBrush="{StaticResource PrimaryBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" Click="UpgradeButton_Click"/>
                                                    </Grid>
                                                    <StackPanel Grid.Row="1" VerticalAlignment="Bottom">
                                                        <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=SubscriptionTierName, FallbackValue='N/A'}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource SuccessBrush}"/>
                                                        <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=SubscriptionExpiryInfo, FallbackValue='Hết hạn: N/A'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                                                    </StackPanel>
                                                </Grid>
                                            </Border>

                                            <!-- Video hôm nay -->
                                            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource CompactStatCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="Video hôm nay" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=VideoLimitInfo, FallbackValue='N/A'}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=VideoResetTimeInfo, FallbackValue='...'}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Dịch Truyện/API bên thứ 3 -->
                                            <Border Grid.Row="0" Grid.Column="2" Style="{StaticResource CompactStatCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="Dịch Truyện/API bên thứ 3" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=RequestLimitInfo, FallbackValue='N/A'}" FontSize="20" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="lượt còn lại" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Ký tự dịch Server -->
                                            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource CompactStatCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="Ký tự dịch Server" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                                    <TextBlock TextWrapping="Wrap" Text="{Binding Source={x:Static local:App.User}, Path=AioCharacterInfo, FallbackValue='N/A'}" FontSize="16" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="dùng trong ngày" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Dịch Phụ Đề API bên thứ 3 -->
                                            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource CompactStatCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="Dịch Phụ Đề API bên thứ 3" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=SrtLimitInfo, FallbackValue='N/A'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="còn lại trong ngày" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>

                                            <!-- Dịch Phụ Đề VIP -->
                                            <Border Grid.Row="1" Grid.Column="2" Style="{StaticResource CompactStatCardStyle}">
                                                <StackPanel>
                                                    <TextBlock Text="Dịch Phụ Đề VIP" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,4"/>
                                                    <TextBlock Text="{Binding Source={x:Static local:App.User}, Path=LocalSrtLimitInfo, FallbackValue='N/A'}" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}"/>
                                                    <TextBlock Text="còn lại trong ngày" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Border>
                                        </Grid>

                                        <!-- Truy cập nhanh -->
                                        <Border Style="{StaticResource DashboardCardStyle}" Margin="8,24,8,8">
                                            <!-- ... nội dung phần Truy Cập Nhanh giữ nguyên như cũ ... -->
                                            <StackPanel>
                                                <TextBlock Text="Truy Cập Nhanh" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,16"/>
                                                <WrapPanel>
                                                    <Button x:Name="SubPhimButton_Dashboard" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                                        <Grid>
                                                            <Viewbox Stretch="UniformToFill">
                                                                <Image Source="/Resources/video.png"/>
                                                            </Viewbox>
                                                            <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                                <TextBlock Text="Phụ Đề" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                            </Border>
                                                        </Grid>
                                                    </Button>
                                                    <Button x:Name="DichThuatButton_Dashboard" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                                        <Grid>
                                                            <Viewbox Stretch="UniformToFill">
                                                                <Image Source="/Resources/dich.png"/>
                                                            </Viewbox>
                                                            <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                                <TextBlock Text="AI Translate" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                            </Border>
                                                        </Grid>
                                                    </Button>
                                                    <Button x:Name="TtsButton_Dashboard" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                                        <Grid>
                                                            <Viewbox Stretch="UniformToFill">
                                                                <Image Source="/Resources/TTS.png"/>
                                                            </Viewbox>
                                                            <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                                <TextBlock Text="AIO Text to Speech Free" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                            </Border>
                                                        </Grid>
                                                    </Button>
                                                    <Button x:Name="OcrTruyenButton_Dashboard" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                                        <Grid>
                                                            <Viewbox Stretch="UniformToFill">
                                                                <Image Source="/Resources/ocr.png"/>
                                                            </Viewbox>
                                                            <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                                <TextBlock Text="OCR - Comic" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                            </Border>
                                                        </Grid>
                                                    </Button>
                                                    <Button x:Name="JianyingButton_Dashboard" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                                        <Grid>
                                                            <Viewbox Stretch="UniformToFill">
                                                                <Image Source="/Resources/jianying.png"/>
                                                            </Viewbox>
                                                            <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                                <TextBlock Text="JIANYING Crack" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                            </Border>
                                                        </Grid>
                                                    </Button>
                                                    <Button x:Name="CapcutButton_Dashboard" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                                        <Grid>
                                                            <Viewbox Stretch="UniformToFill">
                                                                <Image Source="/Resources/capcut.png"/>
                                                            </Viewbox>
                                                            <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                                <TextBlock Text="CapCut Crack" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                            </Border>
                                                        </Grid>
                                                    </Button>
                                                    <Button x:Name="Veo3Button_Dashboard" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                                        <Grid>
                                                            <Viewbox Stretch="UniformToFill">
                                                                <Image Source="/Resources/veo3.png"/>
                                                            </Viewbox>
                                                            <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                                <TextBlock Text="VEO3 Video Generator" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                            </Border>
                                                        </Grid>
                                                    </Button>
                                                </WrapPanel>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </ScrollViewer>
                            </Grid>

                            <Grid x:Name="FeaturesView" Visibility="Collapsed">
                            <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="14,24,14,24" Style="{StaticResource ModernScrollViewerStyle}">
                                <StackPanel>
                                    <TextBlock Text="Danh Sách Tính Năng" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="10,0,0,24"/>
                                    <WrapPanel HorizontalAlignment="Left">
                                        <Button x:Name="SubPhimButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/video.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="AIO Subtitles - Translate Captions" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="DichThuatButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/dich.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="AIO Translate" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="TtsButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/TTS.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="AIO Text to Speech Free" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="OcrTruyenButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/ocr.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="AIO OCR - Comic" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="WatchMovieButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/phim.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="Xem Phim" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="CapcutButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/capcut.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="CapCut Pro" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="JianyingButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/jianying.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="JIANYING PRO" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="EditTruyenButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/edit.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="Edit Truyện" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="Veo3Button_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Viewbox Stretch="UniformToFill">
                                                    <Image Source="/Resources/veo3.png"/>
                                                </Viewbox>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="VEO3 - AI Video Generator" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                        <Button x:Name="CapCutVoiceButton_Features" Style="{StaticResource BannerButtonStyle}" Click="FeatureButton_Click" IsEnabled="{Binding Source={x:Static local:App.User}, Path=IsLoggedIn}">
                                            <Grid>
                                                <Grid Background="#FF6C5CE7">
                                                    <TextBlock Text="&#xE8D6;" FontFamily="Segoe MDL2 Assets, Segoe UI Symbol, Arial" FontSize="80" Foreground="White" Opacity="0.3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                </Grid>
                                                <Border VerticalAlignment="Bottom" Background="#99000000" CornerRadius="0,0,7,7">
                                                    <TextBlock Text="Tạo Voice CapCut" Foreground="White" FontSize="14" FontWeight="SemiBold" TextAlignment="Center" Padding="8,6"/>
                                                </Border>
                                            </Grid>
                                        </Button>
                                    </WrapPanel>
                                </StackPanel>
                            </ScrollViewer>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
            <Border x:Name="NotificationBar"
        Grid.RowSpan="2" 
                Background="{StaticResource BackgroundLightBrush}"
        BorderThickness="1"
        CornerRadius="12"
        Padding="20,16"
        Margin="0,20,0,0"
        HorizontalAlignment="Center"
        VerticalAlignment="Top"
        Visibility="Collapsed"
        Panel.ZIndex="1000">
                <Border.Effect>
                    <DropShadowEffect x:Name="NotificationShadow" ShadowDepth="0" BlurRadius="20" Opacity="0.7"/>
                </Border.Effect>
                <Border.RenderTransform>
                    <TranslateTransform Y="-150"/>
                </Border.RenderTransform>
                <TextBlock x:Name="NotificationText"
                          Text="NOTIFICATION MESSAGE"
                          FontSize="14"
                          FontWeight="SemiBold"
                          Foreground="{StaticResource TextPrimaryBrush}"
                          TextAlignment="Center"/>
            </Border>

                <Border x:Name="ChangePasswordOverlay" 
        Grid.RowSpan="2" 
        Background="#D0000000" 
        Visibility="Collapsed" 
        Panel.ZIndex="1001">
                    <Border Style="{StaticResource DashboardCardStyle}" VerticalAlignment="Center" HorizontalAlignment="Center" MinWidth="450" MaxWidth="500">
                        <StackPanel>
                        <TextBlock Text="Đổi Mật Khẩu" FontSize="24" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" Margin="0,0,0,24"/>
                        <TextBlock Text="Mật khẩu hiện tại" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="CurrentPasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,16"/>
                        <TextBlock Text="Mật khẩu mới (tối thiểu 6 ký tự)" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="NewPasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,16"/>
                        <TextBlock Text="Xác nhận mật khẩu mới" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}" Margin="0,0,0,6"/>
                        <PasswordBox x:Name="ConfirmNewPasswordBox" Style="{StaticResource ModernPasswordBoxStyle}" Margin="0,0,0,16"/>
                        <TextBlock x:Name="ChangePasswordStatusText" Foreground="{StaticResource ErrorBrush}" FontSize="12" TextAlignment="Center" TextWrapping="Wrap" Margin="0,0,0,16" Visibility="Collapsed"/>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="CancelChangePasswordButton" Grid.Column="0" Content="Hủy Bỏ" Style="{StaticResource ModernButtonStyle}" Margin="0,0,8,0" Click="CancelChangePasswordButton_Click"/>
                            <Button x:Name="SubmitChangePasswordButton" Grid.Column="1" Content="Lưu Thay Đổi" Style="{StaticResource PrimaryButtonStyle}" Margin="8,0,0,0" Click="SubmitChangePasswordButton_Click"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Border>

            <Border x:Name="UpdateBanner"
        Grid.RowSpan="2"
        HorizontalAlignment="Center"
        MaxWidth="900"
        Background="{StaticResource BackgroundLightBrush}"
        BorderBrush="{StaticResource WarningBrush}"
        BorderThickness="1"
        VerticalAlignment="Bottom"
        Padding="20,16"
        Margin="20"
        CornerRadius="12"
        Visibility="{Binding Source={x:Static local:App.Updater}, Path=IsUpdateAvailable, Converter={StaticResource BoolToVis}}"
        Panel.ZIndex="999">
                <Border.Effect>
                    <DropShadowEffect ShadowDepth="0" BlurRadius="20" Opacity="0.5" Color="{StaticResource WarningColor}"/>
                </Border.Effect>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets, Segoe UI Symbol, Arial" FontSize="20" VerticalAlignment="Center" Margin="0,0,16,0" Foreground="{StaticResource WarningBrush}"/>
                        <TextBlock VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" Foreground="{StaticResource TextPrimaryBrush}">
                <Run Text="Đã có phiên bản mới"/>
                <Run Text="{Binding Source={x:Static local:App.Updater}, Path=LatestVersion, StringFormat=' ({0})'}" FontWeight="Bold" Foreground="{StaticResource WarningBrush}"/>
                <Run Text="sẵn sàng để cài đặt!"/>
                        </TextBlock>
                    </StackPanel>
                    <Button Grid.Column="1" Content="Cập nhật ngay" Style="{StaticResource ModernButtonStyle}" Background="White" Foreground="Black" BorderBrush="Transparent" FontWeight="Bold" Padding="20,10" Click="UpdateNowButton_Click"/>
                </Grid>
            </Border>
        </Grid>
            
    </Border>
    </Grid>
</Window>