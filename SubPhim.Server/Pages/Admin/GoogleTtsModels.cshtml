@page
@model SubPhim.Server.Pages.Admin.GoogleTtsModelsModel
@{
    ViewData["Title"] = "Quản lý Cấu hình Google TTS Models";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

@if (!string.IsNullOrEmpty(Model.SuccessMessage))
{
    <div class="alert alert-success" role="alert">@Model.SuccessMessage</div>
}
@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger" role="alert">@Model.ErrorMessage</div>
}

<h1>@ViewData["Title"]</h1>
<p class="text-muted">Quản lý cấu hình giới hạn miễn phí và giá cho các loại model Google Cloud TTS.</p>
<hr />

<div class="card">
    <div class="card-header">
        <h5 class="mb-0">Danh sách Cấu hình Model (@Model.ModelConfigs.Count)</h5>
    </div>
    <div class="card-body p-0">
        <div class="table-responsive">
            <table class="table table-striped table-hover mb-0">
                <thead>
                    <tr>
                        <th>Model Type</th>
                        <th>Model Identifier</th>
                        <th class="text-end">Giới hạn miễn phí/tháng</th>
                        <th class="text-end">Giá sau giới hạn</th>
                        <th class="text-center">SSML</th>
                        <th class="text-center">Speaking Rate</th>
                        <th class="text-center">Pitch</th>
                        <th class="text-center">Trạng thái</th>
                        <th>Mô tả</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var config in Model.ModelConfigs)
                    {
                        <tr>
                            <td><strong>@config.ModelType</strong></td>
                            <td><code>@config.ModelIdentifier</code></td>
                            <td class="text-end">
                                @if (config.MonthlyFreeLimit >= 1_000_000)
                                {
                                    <span>@((config.MonthlyFreeLimit / 1_000_000).ToString("N0"))M ký tự</span>
                                }
                                else
                                {
                                    <span>@config.MonthlyFreeLimit.ToString("N0") ký tự</span>
                                }
                            </td>
                            <td class="text-end">$@config.PricePerMillionChars.ToString("N2")/1M</td>
                            <td class="text-center">
                                @if (config.SupportsSsml)
                                {
                                    <i class="bi bi-check-circle-fill text-success"></i>
                                }
                                else
                                {
                                    <i class="bi bi-x-circle-fill text-danger"></i>
                                }
                            </td>
                            <td class="text-center">
                                @if (config.SupportsSpeakingRate)
                                {
                                    <i class="bi bi-check-circle-fill text-success"></i>
                                }
                                else
                                {
                                    <i class="bi bi-x-circle-fill text-danger"></i>
                                }
                            </td>
                            <td class="text-center">
                                @if (config.SupportsPitch)
                                {
                                    <i class="bi bi-check-circle-fill text-success"></i>
                                }
                                else
                                {
                                    <i class="bi bi-x-circle-fill text-danger"></i>
                                }
                            </td>
                            <td class="text-center">
                                @if (config.IsEnabled)
                                {
                                    <span class="badge bg-success">Hoạt động</span>
                                }
                                else
                                {
                                    <span class="badge bg-secondary">Tắt</span>
                                }
                            </td>
                            <td><small>@config.Description</small></td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="mt-3">
    <p class="text-info">
        <i class="bi bi-info-circle"></i> Các cấu hình này được khởi tạo từ dữ liệu seed của database.
        Để thay đổi, vui lòng cập nhật trong AppDbContext và tạo migration mới.
    </p>
</div>
