<Window x:Class="subphimv1.PromptEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:subphimv1"
        xmlns:converters="clr-namespace:subphimv1.Converters"
        mc:Ignorable="d"
        Title="Trình Chỉnh Sửa Prompt" Height="500" Width="700"
        MinHeight="450" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True"
        Background="{StaticResource WindowBackgroundColor}"
        Foreground="{StaticResource TextColor}"
        FontFamily="Segoe UI">
    <Window.Resources>
        <converters:BoolToPromptBehaviorConverter x:Key="BoolToPromptConverter"/>
    </Window.Resources>

    <Border BorderBrush="{StaticResource AccentColor}" BorderThickness="1" CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0" MouseDown="TitleBar_MouseDown">
                <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource WindowCloseButtonStyle}" Content="✕" Click="CloseButton_Click" Width="50" FontSize="14"/>
                </StackPanel>
            </Grid>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="20,10,20,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <GroupBox Header="Cài đặt Prompt" Grid.Row="0" >
                    <StackPanel>
                        <CheckBox x:Name="chkUseSystemBasePrompt" Content="Sử dụng prompt hệ thống theo thể loại đã chọn"/>
                        <TextBlock Margin="20,5,0,0" Foreground="{StaticResource TextSecondaryColor}" TextWrapping="Wrap">
                            <Run Text="Prompt của bạn"/>
                            <Run Text="{Binding IsChecked, ElementName=chkUseSystemBasePrompt, Converter={StaticResource BoolToPromptConverter}}" FontWeight="SemiBold"/>
                        </TextBlock>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="Prompt của bạn (User Prompt)" Grid.Row="1">
                    <TextBox x:Name="txtUserPrompt" 
                             AcceptsReturn="True" 
                             TextWrapping="Wrap" 
                             VerticalScrollBarVisibility="Auto" 
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled" Height="287"/>
                </GroupBox>
            </Grid>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="20,10,20,20">
                <Button Content="Hủy" Width="100" IsCancel="True" Click="CancelButton_Click" Margin="0,0,10,0"/>
                <Button Content="Lưu" Width="100" IsDefault="True" Click="SaveButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>