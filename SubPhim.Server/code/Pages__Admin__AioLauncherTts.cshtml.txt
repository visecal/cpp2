@page
@model SubPhim.Server.Pages.Admin.AioLauncherTtsModel
@{
    ViewData["Title"] = "Quản lý AIOLauncher TTS";
    Layout = "~/Pages/Shared/_AdminLayout.cshtml";
}

@if (!string.IsNullOrEmpty(Model.SuccessMessage))
{
    <div class="alert alert-success" role="alert">@Model.SuccessMessage.Replace("\n", "<br />")</div>
}
@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger" role="alert">@Model.ErrorMessage.Replace("\n", "<br />")</div>
}

<h1>@ViewData["Title"]</h1>
<p class="text-muted">Quản lý các Service Account (SA) dùng cho dịch vụ AIOLauncher Text-to-Speech. Hệ thống sẽ tự động xoay vòng và theo dõi hạn mức 1 triệu ký tự/tháng cho mỗi SA.</p>

<div class="row">
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                Thêm Service Account mới
            </div>
            <div class="card-body">
                <form method="post" asp-page-handler="AddKeys">
                    <div class="mb-3">
                        <label asp-for="JsonKeysInput" class="form-label"></label>
                        <textarea asp-for="JsonKeysInput" class="form-control" rows="10" placeholder="Dán nội dung của một hoặc nhiều file service-account.json vào đây."></textarea>
                        <div class="form-text">Mỗi file JSON sẽ được tự động nhận dạng và xử lý.</div>
                    </div>
                    <button type="submit" class="btn btn-primary w-100">
                        <i class="bi bi-plus-circle-fill me-2"></i>Thêm Keys
                    </button>
                </form>
            </div>
        </div>
    </div>
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                Danh sách Service Account hiện có (@Model.ServiceAccounts.Count)
            </div>
            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped table-hover mb-0">
                        <thead>
                            <tr>
                                <th>Project ID</th>
                                <th>Client Email</th>
                                <th class="text-center">Đã dùng / Tháng</th>
                                <th class="text-center">Trạng thái</th>
                                <th class="text-center">Thao tác</th>
                            </tr>
                        </thead>
                        <tbody>
                            @if (!Model.ServiceAccounts.Any())
                            {
                                <tr>
                                    <td colspan="5" class="text-center text-muted p-4">Chưa có Service Account nào.</td>
                                </tr>
                            }
                            @foreach (var sa in Model.ServiceAccounts)
                            {
                                var usagePercentage = (double)sa.CharactersUsed / 1_000_000 * 100;
                                var progressBarClass = usagePercentage > 90 ? "bg-danger" : (usagePercentage > 70 ? "bg-warning" : "bg-success");
                                <tr>
                                    <td><small><code>@sa.ProjectId</code></small></td>
                                    <td><small>@sa.ClientEmail</small></td>
                                    <td style="min-width: 200px;">
                                        <div class="progress" title="@sa.CharactersUsed.ToString("N0") / 1,000,000 ký tự">
                                            <div class="progress-bar @progressBarClass" role="progressbar" style="width: @usagePercentage%;" aria-valuenow="@usagePercentage" aria-valuemin="0" aria-valuemax="100">
                                                @sa.CharactersUsed.ToString("N0")
                                            </div>
                                        </div>
                                    </td>
                                    <td class="text-center">
                                        @if (sa.IsEnabled)
                                        {
                                            <span class="badge bg-success">Hoạt động</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-secondary">Tắt</span>
                                        }
                                    </td>
                                    <td class="text-center">
                                        <form method="post" asp-page-handler="Delete" asp-route-id="@sa.Id" onsubmit="return confirm('Bạn có chắc muốn xóa vĩnh viễn Service Account này?');">
                                            <button type="submit" class="btn btn-sm btn-danger">Xóa</button>
                                        </form>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>